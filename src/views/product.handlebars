<a href="/view/home">Inicio</a>
<br>
<a id="linkCart" href="" hidden>Ver carrito</a>
<div class="product-details" style="margin-left: 10px;">
    <h2>{{product.code}} - {{product.title}}</h2>

    <div>
        <p>{{product.description}}</p>
        <p>Categoria: {{product.category}}</p>
        <p>Precio: ${{product.price}}</p>
        <p>Stock: {{product.stock}}</p>
        <button id="{{product._id}}" class="button-addCart">Agregar al carrito</button>
    </div>
</div>

<script src="/js/home.js"></script>